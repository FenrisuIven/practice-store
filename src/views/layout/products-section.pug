include components/star.pug
include components/product-card.pug

.container.section-container 
  span.section-title #{category}
  if prods.length > 0
    .products-container
      for product in prods
        + productCard(product)
    if (displayMoreButton)
      button.button.outlined.more(width="35px" height="42px") 
        span Show More

  else
    h2 No product were created yet